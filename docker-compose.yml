services:
  dev:
    build: .
    volumes:
      - .:/app
    stdin_open: true
    tty: true

  lint:
    build: .
    command: >
      sh -c "uv run ruff check pychd &&
             uv run ruff format --check pychd &&
             uv run pyrefly check pychd"
